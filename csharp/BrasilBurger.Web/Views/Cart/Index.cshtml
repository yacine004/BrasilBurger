@model BrasilBurger.Web.Models.ViewModels.CartViewModel

@{
    ViewData["Title"] = "Panier";
}

<h1>🛒 Votre Panier</h1>

@if (Model.Items.Any())
{
    <div class="table-responsive mt-4">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Burger</th>
                    <th>Prix</th>
                    <th>Quantité</th>
                    <th>Sous-total</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.BurgerName</td>
                        <td>@item.Price.ToString("C")</td>
                        <td>@item.Quantity</td>
                        <td>@item.Subtotal.ToString("C")</td>
                        <td>
                            <form method="post" action="/cart/removeitem" style="display:inline;">
                                <input type="hidden" name="burgerId" value="@item.BurgerId">
                                <button type="submit" class="btn btn-sm btn-danger">Supprimer</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="row mt-4">
        <div class="col-md-8"></div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Résumé du Panier</h5>
                    <hr>
                    <div class="d-flex justify-content-between mb-2">
                        <span>Sous-total:</span>
                        <span>@Model.Total.ToString("C")</span>
                    </div>
                    @if (Model.Discount > 0)
                    {
                        <div class="d-flex justify-content-between mb-2 text-success">
                            <span>Réduction:</span>
                            <span>-@Model.Discount.ToString("C")</span>
                        </div>
                    }
                    <hr>
                    <div class="d-flex justify-content-between mb-3" style="font-size: 1.2rem; font-weight: bold;">
                        <span>Total:</span>
                        <span>@Model.TotalAfterDiscount.ToString("C")</span>
                    </div>
                    <a href="/checkout" class="btn btn-primary w-100 mb-2">💳 Procéder au Paiement</a>
                    <a href="/burgers" class="btn btn-secondary w-100">← Continuer les Achats</a>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <form method="post" action="/cart/clear">
            <button type="submit" class="btn btn-outline-danger" onclick="return confirm('Êtes-vous sûr?')">
                🗑️ Vider le Panier
            </button>
        </form>
    </div>
}
else
{
    <div class="alert alert-info mt-4" role="alert">
        <h4 class="alert-heading">Votre panier est vide</h4>
        <p>Parcourez notre menu pour ajouter des burgers délicieux à votre panier!</p>
        <a href="/burgers" class="btn btn-primary">🍔 Voir le Menu</a>
    </div>
}
