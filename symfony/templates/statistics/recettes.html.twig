{% extends "base.html.twig" %}

{% block title %}Recettes - Brasil Burger{% endblock %}

{% block content %}
    <div class="card">
        <h2>💰 Recettes Journalières</h2>
        <p>Date: <strong>{{ "now"|date("d/m/Y") }}</strong></p>
        <a href="{{ path('app_statistics_index') }}" class="btn btn-secondary">← Retour</a>
    </div>

    <div class="stat-card" style="background: linear-gradient(135deg, #d32f2f 0%, #b71c1c 100%); color: white; padding: 30px; text-align: center; margin-bottom: 20px;">
        <h3 style="color: white; margin-bottom: 10px;">Recettes Totales du Jour</h3>
        <div style="font-size: 48px; font-weight: bold;">{{ (recettes ?? 0)|number_format(0, ',', ' ') }} FCFA</div>
    </div>

    <div class="card">
        <h3>📋 Paiements Enregistrés</h3>
        {% if paiements is empty %}
            <p>Aucun paiement enregistré aujourd'hui.</p>
        {% else %}
            <table>
                <thead>
                    <tr>
                        <th>Commande ID</th>
                        <th>Client</th>
                        <th>Montant</th>
                        <th>Mode</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    {% for paiement in paiements %}
                        {% if paiement.paiement %}
                            <tr>
                                <td>#{{ paiement.id }}</td>
                                <td>{{ paiement.client.prenom }} {{ paiement.client.nom }}</td>
                                <td>{{ (paiement.paiement.montant ?? 0)|number_format(2, ',', ' ') }} FCFA</td>
                                <td>{{ paiement.paiement.mode }}</td>
                                <td>{{ paiement.paiement.date|date("d/m/Y H:i") }}</td>
                            </tr>
                        {% endif %}
                    {% endfor %}
                </tbody>
            </table>
        {% endif %}
    </div>
{% endblock %}
