{% extends 'base.html.twig' %}

{% block title %}Gestion des Burgers{% endblock %}

{% block content %}
<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">Gestion des Burgers</h1>
        <a href="{{ path('app_burger_create') }}" class="btn btn-primary">
            <i class="fas fa-plus"></i> Nouveau Burger
        </a>
    </div>

    <div class="row g-4">
        <div class="col-lg-4 col-md-6">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height: 200px;">
                    <i class="fas fa-hamburger fa-3x text-muted"></i>
                </div>
                <div class="card-body">
                    <h5 class="card-title">Aucun burger</h5>
                    <p class="card-text text-muted small">Chargement...</p>
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="h5 mb-0" style="color: var(--primary-color);">-€</span>
                        <span class="badge" style="background: var(--primary-color);">-</span>
                    </div>
                </div>
                <div class="card-footer bg-white border-top-0">
                    <div class="btn-group w-100" role="group">
                        <a href="#" class="btn btn-sm btn-outline-primary">
                            <i class="fas fa-edit"></i> Modifier
                        </a>
                        <button type="button" class="btn btn-sm btn-outline-danger">
                            <i class="fas fa-trash"></i> Supprimer
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
